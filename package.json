{
  "name": "qa-hackathon-playwright-js",
  "version": "1.0.0",
  "scripts": {
    "test": "npx playwright test",
    "test:lambda": "npx playwright test --config=lambdatest.config.js",
    "report": "npx playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.43.0",
    "dotenv": "^16.5.0"
  }
}
